# Dot files archive
Save all of the dot files, like `.bashrc`.

## Install
Use Conda to run
```sh
conda create --name dotf python=3.13
conda activate dotf
python3 -m pip install -r requirements.txt
```

## Todo
- restore files to host machine in right location
